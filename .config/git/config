[user]
  name = Mark Keele
  email = mark@keele.codes
  signingkey = ~/.ssh/git_signing_key.pub

[gpg]
  format = ssh

[core]
  editor = nvim

[merge]
  tool = nvimdiff

[pull]
  rebase = true

[color]
  ui = auto

[init]
	defaultBranch = master

[includeIf "gitdir:~/gridpoint/phoenix/"]
				path = ~/.gitconfig-gridpoint

[includeIf "gitdir:~/gridpoint/site_sim/"]
				path = ~/.gitconfig-gridpoint
